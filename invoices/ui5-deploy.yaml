# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: "3.1"
metadata:
  name: invoices
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        target:
          url: https://s35lp1.ucc.cit.tum.de:8100
          client: '302'
        app:
          name: ZINVOICES
          description: test
          package: zpk_sap490_g09
          transport: S35K901650
        exclude:
          - /test/
